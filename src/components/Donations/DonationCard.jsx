/* eslint-disable no-unused-vars */
import React from "react";

const DonationCard = (dd) => {
  const {
    category,
    category_bg,
    card_bg,
    text_and_button_bg,
    title,
    description,
  } = dd || {};
  console.log(category);
  return <div></div>;
};

export default DonationCard;
